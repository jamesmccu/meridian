# Hosted Applications

This directory is reserved for application manifests managed by GitOps.

Expected future contents:

- Static site deployment manifests.
- Representative services used to exercise observability, security policy, SLOs,
  and deployment workflows.

Application source code should remain in its own repository. This directory should
contain only the desired runtime state for services hosted on Meridian.
